<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TaskbarQuota.FlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:TaskbarQuota.Controls"
    xmlns:loc="using:TaskbarQuota.Localization">

    <Border x:Name="Root"
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
            BackgroundSizing="InnerBorderEdge"
            BorderBrush="{ThemeResource SurfaceStrokeColorFlyoutBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource OverlayCornerRadius}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

        <Frame x:Name="ContentFrame"
               Grid.Row="0"
               Padding="16,12,16,8"
               HorizontalAlignment="Stretch"
               HorizontalContentAlignment="Stretch"
               VerticalAlignment="Stretch"
               VerticalContentAlignment="Stretch"/>

        <Grid x:Name="ActivityPanel"
              Grid.Row="0"
              Padding="16,12,16,8"
              Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
                <TextBlock Text="{loc:Zh Text='Agent activity'}"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           VerticalAlignment="Center"/>
                <ToggleButton x:Name="ActivityMonitoringButton"
                              Grid.Column="1"
                              MinWidth="88"
                              VerticalAlignment="Center"
                              Click="ActivityMonitoringButton_Click"
                              AutomationProperties.Name="{loc:Zh Text='Monitor local agent activity'}"
                              AutomationProperties.AutomationId="FlyoutActivityMonitoringButton"
                              ToolTipService.ToolTip="{loc:Zh Text='Monitor local agent activity'}">
                    <TextBlock Text="{loc:Zh Text=Monitoring}"/>
                </ToggleButton>
                <ToggleButton x:Name="ActivityWidgetButton"
                              Grid.Column="2"
                              MinWidth="76"
                              VerticalAlignment="Center"
                              Click="ActivityWidgetButton_Click"
                              AutomationProperties.Name="{loc:Zh Text='Show agent activity in usage widget'}"
                              AutomationProperties.AutomationId="FlyoutActivityWidgetButton"
                              ToolTipService.ToolTip="{loc:Zh Text='Show agent activity in usage widget'}">
                    <TextBlock Text="{loc:Zh Text='Activity Widget'}"/>
                </ToggleButton>
            </Grid>
            <TextBlock x:Name="ActivityEmptyState"
                       Grid.Row="2"
                       Margin="0,24,0,0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       Text="{loc:Zh Text='No recent agent activity'}"
                       Style="{StaticResource BodyTextBlockStyle}"/>
            <ScrollViewer x:Name="ActivityScrollViewer"
                          Grid.Row="2"
                          Margin="0,12,0,0"
                          AutomationProperties.Name="{loc:Zh Text='Agent activity list'}"
                          AutomationProperties.AutomationId="FlyoutActivityList"
                          AutomationProperties.LiveSetting="Polite"
                          VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ActivityList" Spacing="8"/>
            </ScrollViewer>
        </Grid>

        <controls:UpdateActionBar x:Name="UpdateBar"
                                  Grid.Row="1"
                                  Margin="12,0,12,8"/>

        <StackPanel x:Name="BottomChrome"
                    Grid.Row="2"
                    Margin="12,0,12,12"
                    Spacing="8"
                    VerticalAlignment="Bottom">
            <!-- Quick controls: surface mode + floating Acrylic strength (always available).
                 Opacity slider is created in code — RangeBase Minimum/Maximum cannot be set safely in XAML
                 (defaults Maximum=1; assigning Minimum=35 throws XamlParseException). -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="10,8">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <ToggleButton x:Name="FloatingSurfaceButton"
                                  MinWidth="108"
                                  VerticalAlignment="Center"
                                  Click="FloatingSurfaceButton_Click"
                                  AutomationProperties.Name="{loc:Zh Text='Show usage as floating window'}"
                                  AutomationProperties.AutomationId="FlyoutFloatingSurfaceButton"
                                  ToolTipService.ToolTip="{loc:Zh Text='Show usage as a floating always-on-top window instead of the taskbar'}">
                        <TextBlock x:Name="FloatingSurfaceButtonLabel" Text="{loc:Zh Text=Floating}"/>
                    </ToggleButton>
                    <Grid x:Name="FloatingOpacitySliderHost"
                          Width="160"
                          VerticalAlignment="Center"
                          AutomationProperties.Name="{loc:Zh Text='Floating acrylic strength'}"/>
                    <TextBlock x:Name="FloatingOpacityLabel"
                               MinWidth="40"
                               VerticalAlignment="Center"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Opacity="0.8"
                               Text="90%"/>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal"
                        Spacing="8"
                        HorizontalAlignment="Left">
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="4,0">
                    <StackPanel x:Name="ProviderStrip"
                                MinHeight="48"
                                Orientation="Horizontal"/>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <Button x:Name="ActivityButton"
                            Width="48"
                            Height="48"
                            MinWidth="0"
                            MinHeight="0"
                            Padding="0"
                            BorderThickness="0"
                            Background="Transparent"
                            AutomationProperties.Name="{loc:Zh Text='Agent activity'}"
                            AutomationProperties.AutomationId="FlyoutActivityButton"
                            Click="ActivityButton_Click"
                            ToolTipService.ToolTip="{loc:Zh Text='Agent activity'}">
                        <FontIcon Glyph="&#xE7F4;"
                                  FontSize="16"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Button>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <Button x:Name="CostButton"
                            Width="48"
                            Height="48"
                            MinWidth="0"
                            MinHeight="0"
                            Padding="0"
                            BorderThickness="0"
                            Background="Transparent"
                            AutomationProperties.Name="{loc:Zh Text=Cost}"
                            AutomationProperties.AutomationId="FlyoutCostButton"
                            Click="CostButton_Click"
                            ToolTipService.ToolTip="{loc:Zh Text=Cost}">
                        <FontIcon Glyph="&#xE9D2;"
                                  FontSize="16"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Button>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <Button x:Name="SettingsButton"
                            Width="48"
                            Height="48"
                            MinWidth="0"
                            MinHeight="0"
                            Padding="0"
                            BorderThickness="0"
                            Background="Transparent"
                            AutomationProperties.Name="{loc:Zh Text=Settings}"
                            AutomationProperties.AutomationId="FlyoutSettingsButton"
                            Click="SettingsButton_Click"
                            ToolTipService.ToolTip="{loc:Zh Text=Settings}">
                        <FontIcon Glyph="&#xE713;"
                                  FontSize="16"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Button>
                </Border>
            </StackPanel>
        </StackPanel>
    </Grid>
    </Border>
</Window>
